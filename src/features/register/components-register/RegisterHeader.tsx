import AuthHeader from "@/common/AuthHeader";

export default function RegisterHeader() {
	return (
		<AuthHeader
			title="Buat akun gratis"
			subTitle="Mulai kelola invoice dengan mudah"
		/>
	);
}
